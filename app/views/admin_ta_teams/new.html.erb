<% html_title "New Team" %>

<h2>New Team</h2>

<%= form_for @team, url: admin_ta_teams_path, html: { class: 'tabular' } do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= submit_tag l(:button_create) %>
  <%= link_to l(:button_cancel), admin_ta_teams_path %>
<% end %>
